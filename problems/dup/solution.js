function removeDup(nums) {
    let uniqueNums = [];
    let bool = [];
    let prevNum;


    nums.forEach(num,i => {
       '
       '

        if (uniqueNums.length === 0) {
            uniqueNums.push(num);
        }
        else {
            if (nums)
        }
        // if (prevNum !== num) {

        // }
        // if (bool[num] === undefined)
        //     bool[num] = false;
        // else if (bool[num] === false)
        //     bool[num] = true;
        // prevNum = num;
    })
}
